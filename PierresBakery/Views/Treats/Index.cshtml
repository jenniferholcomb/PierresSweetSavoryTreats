@{
  Layout = "_Layout";
}

@using PierresBakery.Models;

<h1>Treats</h1>

@*@if(@Model.Count == 0)
{
  <h3>No treats have been added yet.</h3>
}
else
{
  <ul>
    @foreach (Treat treat in Model)
    {
      <li>@Model.Treat.Name</li>
    }
  </ul>
}*@

<p>@Html.ActionLink("Add new treat", "Create")</p>
<p>@Html.ActionLink("Home", "Index", "Home")</p>